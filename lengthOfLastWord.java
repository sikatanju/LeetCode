public class lengthOfLastWord   {
    /*public int findLength (String str)  {
        char[] ch = str.toCharArray();
        int i = ch.length-1;
        while (i>=0 && ch[i] == ' ')
            i--;
        
        int count = 0;
        while(i>=0 && ch[i] != ' ') {
            i--;
            count++;
        }
        return count;
    } */
}